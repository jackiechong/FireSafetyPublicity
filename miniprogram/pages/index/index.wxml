<view class="home">
  <view class="hero">
    <image wx:if="{{bannerImageUrl}}" class="hero-bg" src="{{bannerImageUrl}}" mode="aspectFill" />
    <view wx:if="{{!bannerImageUrl}}" class="hero-title">葫消培</view>
    <view wx:if="{{!bannerImageUrl}}" class="hero-main">关注消防 生命至上</view>
    <view wx:if="{{!bannerImageUrl}}" class="hero-sub">全民消防 生命至上</view>
    <view wx:if="{{!bannerImageUrl}}" class="hero-scene">
      <view class="sea"></view>
      <view class="city"></view>
      <view class="mascot">
        <view class="helmet"></view>
        <view class="face"></view>
        <view class="body"></view>
      </view>
    </view>
  </view>

  <view class="module-card">
    <view
      wx:for="{{modules}}"
      wx:key="key"
      class="module-item"
      data-index="{{index}}"
      bindtap="onPickModule"
    >
      <view class="module-icon {{item.color}}">{{item.icon}}</view>
      <view class="module-title">{{item.title}}</view>
    </view>
  </view>

  <view class="notice-card">
    <text class="notice-label">通知公告</text>
    <text class="notice-text">培训活动、学习内容与签到记录服务已开放</text>
    <text class="notice-date">07-30</text>
  </view>

  <view id="activities" class="section-head">
    <text>热门活动</text>
    <text class="more" bindtap="openActivities">更多 ></text>
  </view>
  <view class="activity-row">
    <view wx:for="{{activityArticles}}" wx:key="id" class="activity-card {{index === 0 ? 'warm' : 'cool'}}" bindtap="openActivities">
      <image wx:if="{{item.image_url}}" class="activity-bg" src="{{item.image_url}}" mode="aspectFill" />
      <view class="activity-title">{{item.title}}</view>
      <view class="activity-sub">{{item.content}}</view>
      <view class="activity-link">查看详情</view>
    </view>
  </view>

  <view class="bottom-space"></view>

  <view class="bottom-nav">
    <view class="nav-item active">
      <view class="nav-icon">🏠</view>
      <view class="nav-label">首页</view>
    </view>
    <view class="nav-item" bindtap="goKnowledge">
      <view class="nav-icon">📖</view>
      <view class="nav-label">知识</view>
    </view>
    <view class="nav-item join-nav {{loadingAction === 'join' ? 'loading' : ''}}" bindtap="goJoin">
      <view class="join-icon">
        <text>➕</text>
      </view>
      <view class="nav-label">{{loadingAction === 'join' ? '加载中' : '加入培训'}}</view>
    </view>
    <view class="nav-item" bindtap="openActivities">
      <view class="nav-icon">📅</view>
      <view class="nav-label">活动</view>
    </view>
    <view class="nav-item" bindtap="goMe">
      <view class="nav-icon">👤</view>
      <view class="nav-label">我的</view>
    </view>
  </view>
</view>
